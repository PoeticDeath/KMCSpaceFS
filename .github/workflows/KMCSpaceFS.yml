name: KMCSpaceFS

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

permissions:
  contents: read

jobs:
  windows-build:
  
    runs-on: windows-2022

    steps:
    - uses: actions/checkout@v4
    - name: Setup MSBuild
      uses: microsoft/setup-msbuild@v2
      with:
        msbuild-architecture: "x64"
    - name: Symlink
      run: |
        mkdir "C:\Users\Anthony Kerr\source\repos\PoeticDeath"
        cd "C:\Users\Anthony Kerr\source\repos\PoeticDeath"
        New-Item -ItemType SymbolicLink -Path KMCSpaceFS -Target "D:\a\KMCSpaceFS\KMCSpaceFS"
    - name: Build Driver
      run: msbuild KMCSpaceFS.sln /p:Configuration="Release" /p:Platform=x64
    - name: KMCSpaceFS.sys
      uses: actions/upload-artifact@v4
      with:
        name: KMCSpaceFS.sys
        path: x64\Release\KMCSpaceFS.sys
    - name: KMCSpaceFS.inf
      uses: actions/upload-artifact@v4
      with:
        name: KMCSpaceFS.inf
        path: src\KMCSpaceFS.inf
    - name: KMCSpaceFS-VOL.inf
      uses: actions/upload-artifact@v4
      with:
        name: KMCSpaceFS-VOL.inf
        path: src\KMCSpaceFS-VOL.inf
